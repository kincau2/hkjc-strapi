'use strict';

/**
 * racing-academy lifecycle hooks
 */

module.exports = {
  // Lifecycle hooks can be added here if needed
  // Tags are now handled via relation, so no special processing needed
};

